<Page
    x:Class="AsyncAndAwaitHandlingErrors.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AsyncAndAwaitHandlingErrors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"></RowDefinition>
                <RowDefinition Height="40*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"></ColumnDefinition>
                <ColumnDefinition Width="560"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0" Grid.Column="0" x:Name="OpenFileButton" Click="OpenFileButtonClick">Open File</Button>
            <TextBlock Grid.Row="1" Grid.Column="0" x:Name="FileInfo" FontSize="20" Text="File"></TextBlock>
            <Button Grid.Row="0" Grid.Column="1" x:Name="DownloadPostsButton" Click="DownloadPostsButtonClick">Download Posts</Button>
            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="Posts" FontSize="20" Text="Posts" TextWrapping="Wrap"></TextBlock>
        </Grid>
    </Grid>
</Page>
