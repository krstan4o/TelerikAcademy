<UserControl x:Class="EmailClientWPF.Views.InboxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500">
    <StackPanel>
        <TextBlock>asjdSCBUZ\HBCKSHCVSVAGHVGH</TextBlock>
    <ListBox ItemsSource="{Binding MessagesList}">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Sender}"></TextBlock>
                    <TextBlock Text="{Binding Subject}"></TextBlock>
                    <TextBlock Text="{Binding Date}"></TextBlock>
                </StackPanel>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
    </StackPanel>
</UserControl>
