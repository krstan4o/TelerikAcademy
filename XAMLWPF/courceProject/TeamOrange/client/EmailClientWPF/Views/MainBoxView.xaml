<UserControl x:Class="EmailClientWPF.Views.MainMailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:EmailClientWPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Button Width="50" Height="20" Margin="240,10,10,270"
                Command="{Binding LogOut}">LogOut</Button>
        <StackPanel >
            <Button DockPanel.Dock="Left" Width="150" Height="25" 
                    Command="{Binding CreateMessage}">
                NewMessage
            </Button>
            <Button DockPanel.Dock="Left" Width="150" Height="25" 
                    Command="{Binding GetInbox}">
               Inbox
            </Button>
            <Button DockPanel.Dock="Left" Width="150" Height="25" 
                    Command="{Binding GetSended}">
               Sent
            </Button>
            <Button DockPanel.Dock="Left" Width="150" Height="25" 
                    Command="{Binding GetTrash}">
                Trash
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
